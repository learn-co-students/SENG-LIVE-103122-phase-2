// - Return the `Header`, `ProjectForm` and `ProjectList` components

// - Inport / provide the array of `projects` to `ProjectList` as props

function App() {
  return <div className="App">Project Showcase</div>;
}

export default App;
